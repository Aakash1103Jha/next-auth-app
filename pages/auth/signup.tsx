import { NextPage } from "next";

import styles from "../../styles/SignUp.module.css";

const SignUp: NextPage = (props) => {
	return <div className={`${styles.signUp_page}`}></div>;
};

export default SignUp;
